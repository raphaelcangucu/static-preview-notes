---
interface Props {
  state: 'active' | 'partial' | 'locked' | 'absent';
  label: string;
}

const { state, label } = Astro.props;
---

<span class="access-state" data-state={state}>
  <span class="access-state__mark" aria-hidden="true"></span>
  {label}
</span>
